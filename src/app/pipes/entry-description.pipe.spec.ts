import { EntryDescriptionPipe } from './entry-description.pipe';

describe('EntryDescriptionPipe', () => {
	it('create an instance', () => {
		const pipe = new EntryDescriptionPipe();
		expect(pipe).toBeTruthy();
	});
});
